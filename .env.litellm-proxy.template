# Copy this file to .env.litellm-proxy, fill in the API keys, and run `source
# .env.litellm-proxy` to set the environment variables. Then start the server
# with `docker compose up`.
export OPENAI_API_KEY=
export ANTHROPIC_API_KEY=
export GEMINI_API_KEY=
